{% extends 'base.html.twig' %}

{% block title %}Details{% endblock %}

{% block body %}
    <h1>Details</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detail.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ detail.quantite }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ detail.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('details_index') }}">back to list</a>

    <a href="{{ path('details_edit', {'id': detail.id}) }}">edit</a>

    {{ include('details/_delete_form.html.twig') }}
{% endblock %}
