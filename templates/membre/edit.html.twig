{% extends 'base.html.twig' %}

{% block title %}
    {% if is_granted("ROLE_ADMIN") %}
        Modifier le membre
    {% else %}
        Modifier mon profil
    {% endif %}
{% endblock %}

{% block body %}
    <h1>{{ is_granted("ROLE_ADMIN") ? " Modifier le membre" : "Modifier mon profil" }}</h1>

    {{ include('membre/_form.html.twig', {'button_label': 'Update'}) }}

    {% if is_granted("ROLE_ADMIN") %}
        <a href="{{ path('membre_index') }}">back to list</a>
    {% endif %}

    {{ include('membre/_delete_form.html.twig') }}
{% endblock %}
